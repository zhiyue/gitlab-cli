# frozen_string_literal: true
#
# Placeholder — this file is regenerated by scripts/update-homebrew-formula.sh
# on every release tag (run automatically by .github/workflows/release.yml).
# After v0.1.0 lands, this comment block is replaced with the real formula.

class GitlabCli < Formula
  desc "Agent-first CLI for legacy GitLab EE 14.0.5"
  homepage "https://github.com/zhiyue/gitlab-cli"
  license "Apache-2.0"
  version "0.0.0-placeholder"

  def install
    odie "Formula not yet generated — wait for the next release tag."
  end
end
